import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  image: {
    width: 170,
    resizeMode: "contain",
  },
  subtitle: {
    fontSize: 20,
    marginTop: -20,
    color: "#ABABAB",
  },
});
